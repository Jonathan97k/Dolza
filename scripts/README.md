# Seed Script

Run `npm run seed` to populate MongoDB with initial data.
Requires MONGODB_URI in .env file.

To copy images from the old PHP project:
XCOPY /E /I path\to\old-project\public\images F:\PROJECTS\Dolza\images
